name: Release

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type (only for manual triggers)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
          - auto

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    # Only run if tests passed (or manual trigger)
    if: |
      github.event_name == 'workflow_dispatch' || 
      github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: write
      id-token: write
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Need full history for standard-version
        token: ${{ secrets.GITHUB_TOKEN }}
    
    # Check if this is a dependabot commit and skip if so
    - name: Check for dependabot
      run: |
        LAST_COMMIT_MSG=$(git log -1 --pretty=format:'%s')
        LAST_COMMIT_AUTHOR=$(git log -1 --pretty=format:'%an')
        echo "Last commit author: $LAST_COMMIT_AUTHOR"
        echo "Last commit message: $LAST_COMMIT_MSG"
        
        # Skip if it's a dependabot commit (author or commit message pattern)
        if [[ "$LAST_COMMIT_AUTHOR" == "dependabot[bot]" ]] || [[ "$LAST_COMMIT_MSG" =~ ^chore\(deps.*\): ]]; then
          echo "This is a dependency update commit. Skipping release."
          echo "::notice::Skipping release for dependency update"
          # Create a dummy file to signal other steps to skip
          touch .skip-release
          exit 0
        fi
    
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
        registry-url: 'https://registry.npmjs.org'
        cache: 'npm'
    
    - name: Configure git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
    
    - run: npm ci
    
    # Determine release type
    - name: Determine release type
      id: release_type
      run: |
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          # Manual trigger - use input
          RELEASE_TYPE="${{ github.event.inputs.release_type }}"
        else
          # Automatic trigger - let standard-version decide based on commits
          RELEASE_TYPE="auto"
        fi
        echo "RELEASE_TYPE=$RELEASE_TYPE" >> $GITHUB_OUTPUT
    
    # Run standard-version to bump version and generate changelog
    - name: Bump version and generate changelog
      run: |
        RELEASE_TYPE="${{ steps.release_type.outputs.RELEASE_TYPE }}"
        if [ "$RELEASE_TYPE" == "auto" ]; then
          npx standard-version
        else
          npx standard-version --release-as $RELEASE_TYPE
        fi
    
    # Build the project (after version bump so dist has correct version)
    - run: npm run build
    
    # Get the new version
    - name: Get version
      id: version
      run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
    
    # Extract release notes from CHANGELOG.md
    - name: Extract release notes
      run: |
        VERSION="${{ steps.version.outputs.VERSION }}"
        # Extract content between version header and next header (handles both ## and ###)
        NOTES=$(awk '/^##+ \['"$VERSION"'\]/ {flag=1; next} /^#+ \[/ && flag {exit} flag' CHANGELOG.md)
        
        # Save to file to handle multi-line content properly
        echo "$NOTES" > release_notes.md
    
    # Push commits and tags
    - name: Push changes
      run: |
        # The checkout action configures git to use the token
        git push --follow-tags origin main
    
    # Create GitHub release using gh CLI (matches manual process)
    - name: Create GitHub Release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION="${{ steps.version.outputs.VERSION }}"
        gh release create "v$VERSION" \
          --title "v$VERSION" \
          --notes-file release_notes.md
    
    # Publish to NPM with provenance
    - run: npm publish --provenance --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}